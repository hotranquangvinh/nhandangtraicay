services:
  fruit-detector:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: fruit-detector
    ports:
      - "7860:7860"
    environment:
      HOST: 0.0.0.0
      PORT: 7860
      FLASK_DEBUG: 0
      ALLOW_BASE_MODEL: 1
      # Neu co model custom, bo comment dong duoi va doi dung ten file.
      # MODEL_PATH: best.pt
    restart: unless-stopped
